interface ButtonProps {
  text: string,
  destination: string,
}

const NavButton = ({text, destination}: ButtonProps) => {
  // Nice button here with hover effect
  return (
    <></>
  )
}

export default (NavButton)